#include "main.h"

/**
 * _islower - function of a line
 *@c: contains value to be che#include "main.h"
 *Return: Always 0.
 */
int _islower(int c)
{
	if (c > 'a' && c < 'z')
	{
	return (1);
	}
	else
	{
	return (0);
	}

}cked 
 *Return: Always 0.
 */
int _islower(int c)
{
	if (c > 'a' && c < 'z')
	{
	return (1);
	}
	else
	{
	return (0);
	}

}
